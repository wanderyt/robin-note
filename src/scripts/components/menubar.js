import React from 'react';

import * as MenuConfig from '../../config/menu-info.json';

const MenuBar = (props) => {
    getMenus() {
        let menus = [];

    }
    return (
        <div
            className={`MenuBar ${props.classNames}`}>

        </div>
    )
}